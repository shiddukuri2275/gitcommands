git --version

git config --global user.name "your user name"

git config --global user.email "your mail ID "

git init

git clone url of github 

git status

git add filename

git commit -m " description "

git push origin master  ---> used to push our local files to git hub
